<div class='row'>
  <div class='col s12 m10 offset-m1 l8 offset-l2'>
    <div class='card'>
      <!-- Imagen de tarjeta -->
      <div class='card-image'>
        <img src='/images/servicio.jpg' alt='Project Image' />
        <span class='card-title white teal-text text-darken-4'>
          Agregar Alumno
        </span>
      </div>
      <!-- Contenido de tarjeta -->
      <form autocomplete='off' action='/project/add' method='POST'>
        <div class='card-content'>
          <div class='input-field'>
            <label for='project-name'>Nombre del Alumno</label>
            <input
              placeholder='El nombre del alumno aqui 🧑‍💻'
              type='text'
              name='name'
              id='project-name'
              value="{{project.name}}"
              required
            />
            {{#if errorModel.nameError}}
            <span style="color:red;">🚩 {{errorModel.nameError}}</span>
            {{/if}}
          </div>
          <div class='input-field'>
            <label for='project-description'>
              Numero de Control
            </label>
            <textarea
              name='description'
              class="materialize-textarea"
              id='project-description'
              required
            >{{project.description}}</textarea>
            {{#if errorModel.descriptionError}}
            <span style="color:red;">🚩 {{errorModel.descriptionError}}</span>
            {{/if}}
          </div>
          <div class='input-field'>
            <label for='project-area'>
              Area en la que realizara el servicio social
            </label>
            <textarea
              name='area'
              class="materialize-textarea"
              id='project-area'
            >{{project.area}}</textarea>
            {{#if errorModel.area}}
            <span style="color:red;">🚩 {{errorModel.areaError}}</span>
            {{/if}}
          </div>
        </div>
        <div class='card-action'>
          <button type="submit" class="blue darken-3 waves-effect waves-light btn">
            <i class="material-icons left">note_add</i> Agregar
          </button>
        </div>
      </form>
    </div>
  </div>
</div>